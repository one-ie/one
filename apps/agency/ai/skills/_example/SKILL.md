---
name: summarise
description: Summarises any document to the requested length.
model: openrouter/auto
lifecycle: beta
---

# Summarise

Given a document, produce a clear summary at the requested length (short / medium / full).
Default to medium when no length is specified.

Copy this folder to `ai/skills/<slug>/`, rename `SKILL.md`, fill it in, then `one push`.
