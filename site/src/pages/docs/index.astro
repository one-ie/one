---
// docs({ injectRoutes: false }) in one.config.ts — DocsIndex is a bare
// fragment, so this site supplies the actual page via its own Layout.
import Layout from '../../layouts/Layout.astro'
import DocsIndex from '@oneie/plugin-docs/DocsIndex.astro'
---

<Layout title="Docs — ONE" description="Everything you need to get started.">
  <DocsIndex />
</Layout>
