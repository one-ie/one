---
// docs({ injectRoutes: false }) in one.config.ts — DocsIndex is a bare
// fragment, so this site supplies the actual page via its own Layout.
import Layout from '../../layouts/Layout.astro'
import DocsIndex from '@oneie/plugin-docs/DocsIndex.astro'

export const prerender = true
---

<Layout title="ONE Docs — Setup, Plugins, and Workspace Configuration Guides" description="Guides covering how a ONE workspace is provisioned, how its plugins are wired, and how to configure your own instance.">
  <DocsIndex />
</Layout>
