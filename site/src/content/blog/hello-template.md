---
title: "Hello, template — this workspace's first live post"
description: This workspace was provisioned live — a real client, a real API key, a real blog post.
date: 2026-07-08
category: Updates
tags: [oneie, plugins]
---

This site is running on the `template` workspace, provisioned with `one init --name template`. Every plugin on `/plugins` is being wired up and verified one at a time — this post is the proof the blog plugin's `/blog` and `/blog/[slug]` routes actually work end to end.
