---
export const prerender = true
import Layout from '../layouts/Layout.astro'
import { ChatLazy } from '../components/ChatLazy'
---

<Layout title="Chat — ONE" sidebar="mini">
  <div class="h-[100dvh] overflow-hidden">
    <ChatLazy client:only="react" fullPage />
  </div>
</Layout>
