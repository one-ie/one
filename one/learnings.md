# Learnings

> Rolling log of non-obvious insights from `/do` wave and cycle closes.
> Append-only. Never edit existing entries. Keyed by `## <TODO-slug>`.

---

## Schema

```
- YYYY-MM-DD · cycle N · wave N|gate · {one sentence} · rubric=0.NN · source=w1|w2|w3|w4|cycle
```

Each TODO gets its own `## <slug>` section. On each close, append to the matching section.

---

## loop-close

- 2026-04-18 · cycle 1 · gate · Protocol seeded: one signal (do:close), one log (learnings.md), one hard gate — cycle close blocks next cycle until confirmed. · rubric=0.85 · source=cycle
- 2026-04-18 · cycle 2 · w1 · /api/events endpoint does not exist — do:close verification uses /api/signals; gate is protocol-level not code-enforced. · rubric=0.80 · source=w1
- 2026-04-18 · cycle 2 · gate · C2 prove: signal fires end-to-end, learnings grew, hard gate is a model-instruction convention; machine enforcement needs /api/events?receiver=do:close. · rubric=0.80 · source=cycle
- 2026-04-18 · human-agent-link cycle 1 · gate · Path D-minimal: resolveUnitFromSession unifies Bearer + Cookie on existing validateApiKey + auth.api.getSession pipelines. No schema change, no BetterAuth hooks. W1 recon (R4) surfaced adapter FIELD_TO_ATTR flat mapping as the blocker that killed the original additionalFields plan. · rubric=0.91 · source=cycle
- 2026-04-18 · human-agent-link cycle 2 · gate · PROVE shipped: /api/me/agents + /app dashboard + /api/agents/:id/authorize + hasPathRelationship gate on mark/warn + 9-test suite (26 total, 0 fail). W4 flagged TQL injection in raw-interpolated from/to — W3.5 micro-edit sanitized at entry. Key insight: delegation = pheromone on path; no new relation needed (path.scope already exists). · rubric=0.894 · source=cycle
- 2026-04-18 · human-agent-link cycle 3 · gate · GROW shipped: /api/signal scope gate (auth-only, private/group/public), /api/me/groups/:gid/invite (chairman+ced idempotent), chairman-chain uid-prefix heuristic as defense-in-depth. +3 scope tests (27 chairman-chain total). Full suite 1581/1603 pass, 0 regressions. W4 flagged: authed callers omitting scope default to 'group' = breaking change for any prior cross-group senders (by design, per governance spec). · rubric=0.877 · source=cycle
- 2026-04-18 · mcp cycle 1 · gate · WIRE shipped: auth_agent + sync_agent + discover_skill + register + pay in packages/mcp/src/tools/lifecycle.ts. 20 tools total (was 15). W0 also seeded audit-baseline (+14 from chat refactor in prior commit) + biome format fix in specialist-leaf.ts. W4 loop: tutorial-assisted.md had 5 curl fallbacks (incl. troubleshooting table); all replaced. Key pattern: doc gate (curl count=0) is as load-bearing as the build gate. · rubric=0.92 · source=cycle
- 2026-04-18 · mcp cycle 2 · gate · PROVE shipped: claw + commend + flag + status + capabilities_add + capabilities_publish + hire + bounty + escrow_create + harden in commerce.ts. 30 tools total (was 20). Renamed capabilities_list→capabilities_add (endpoint is POST/add, not GET/list). Fixed pre-existing biome import-sort in worlds/tenant.ts + worlds/tenant.ts was introduced by chat refactor. · rubric=0.91 · source=cycle
- 2026-04-19 · mcp cycle 3 · gate · GROW shipped: stats + health + revenue + frontiers_global + hypotheses_global + export_units + export_highways + intent_learn + ingest_event + chat_turn in observability.ts. 40 tools total (was 30). chat_turn uses SSE stream reader (0: prefix lines from Vercel AI SDK data stream). ingest_event routes to /api/ingest/:kind (separate files, not dynamic route). Version bumped to 0.3.0. W3 agents hit usage limit overnight; edits applied directly. · rubric=0.92 · source=cycle
- 2026-04-19 · cli cycle 1 · gate · WIRE shipped: 12 new command files (pay/hire/bounty/commend/flag/status/capabilities/publish/stats/health/revenue/export). 30 verbs total. All 12 have matching MCP tools; 7/12 have SDK methods (hire/bounty/publish/revenue/export gaps tracked in TODO-sdk). cli-reference.md updated. · rubric=0.89 · source=cycle
- 2026-04-19 · cli cycle 2 · gate · PROVE shipped: config (profiles in ~/.config/oneie/profiles/), completion (bash/zsh/fish), output.ts (json/yaml/table formatters), agent --watch (chokidar), --dry-run global flag (hasDryRun in args.ts), doctor (colored diagnostic grid). 6 ergonomic features, backward-compatible. cli-reference.md updated. · rubric=0.89 · source=cycle
- 2026-04-19 · cli cycle 3 · gate · GROW shipped: repl (readline loop, dispatches to existing run() exports), pipe (NDJSON stdin→signal→stdout, concurrent per-line), tail (SSE reader with tag filter), agent <dir> (batch sync). 36 verbs total. W4 rubric=0.889 (fit=0.90 form=0.85 truth=0.92 taste=0.85). tsconfig lib/target mismatch (ES2020 vs ES2022) is latent non-blocking. doctor.ts uses chalk while other commands don't — minor taste inconsistency. · rubric=0.889 · source=cycle
- 2026-04-19 · cycle 3 · wave 3+4 · CF Workers migration docs swept: CLAUDE.md migration note updated, wrangler pages deploy → wrangler deploy across 6 docs, DOMAIN-CONFIG.md sample corrected · rubric=0.80 · source=w3+w4
- 2026-04-19 · sui-phase3 cycle 1 · gate · ESCROW shipped: release/[id].ts + cancel/[id].ts unblocked (were permanently returning 400); TypeQL `(or ...)` bug in settle.ts fixed (silent pheromone drop on every settlement); 14 skeleton tests → 7 real mocked assertions; bridge.ts already had EscrowCreated/Released/Cancelled handlers (E5 pre-done). All core work was pre-built in Phase 2 except 2 live bugs + 2 stub routes. W4: 1574/1596 tests, biome clean, tsc clean. · rubric=0.90 · source=cycle
- 2026-04-19 · llm-routing cycle 1 · gate · WIRE complete: C1 was already implemented (chooseModelLocal + markModelOutcome in substrate.ts, 4 call sites in router.ts). TODO checkboxes were stale. No edits needed — verification confirmed via test suite + code audit. Key insight: pre-existing D1-backed pheromone in nanoclaw can't use llm-router.ts (needs World instance); nanoclaw already has the equivalent natively. · rubric=0.88 · source=cycle
- 2026-04-19 · llm-routing cycle 2 · gate · PROVE shipped: response_cache + call_log D1 tables (migrations/0004_response_cache.sql); hashKey() via crypto.subtle; PRE-LLM cache lookup + POST-LLM quality-gated store (>0.5→cache, >0.7→24h TTL, else 1h); call_log insert on every non-streaming /message call. Cache scope: non-streaming /message only (streaming path excluded for C2). W4: 1574/1596 tests, biome 6 warnings (pre-existing), tsc clean. · rubric=0.88 · source=cycle
- 2026-04-19 · llm-routing cycle 3 · gate · GROW shipped: resolveIntentLocal() D1 keyword match in substrate.ts; intent→cache lookup before LLM call (confidence ≥ 0.80 + response_cache hit → skip LLM); hypothesis recall injection into system prompt (top 2 facts, confidence > 0.5). migrations/0005_intents.sql pre-existed. W3.5: biome format fix on both files (try-catch one-liner + whitespace). W4: 1574/1596, biome clean, tsc clean. · rubric=0.88 · source=cycle
- 2026-04-19 · llm-routing cycle 4 · gate · SUSTAIN shipped: claw_patterns D1 table (migrations/0015_claw_patterns.sql); matchClawPattern() in substrate.ts (60s module cache, regex match); pattern check PRE-intent-resolution in router.ts non-streaming path (reply→canned, block→403, tag→inject). Quality self-measurement via outcome.ts deferred (requires turn_meta pattern_id threading — CF Worker stateless, too complex for C4). W4: 1574/1596, biome clean, tsc clean. · rubric=0.86 · source=cycle
- 2026-04-19 · auth-todo cycle 1 · gate · OWNERSHIP shipped: rate-mint gate (AUTH_AGENT_REMINT_MODE=audit|enforce), claim endpoint POST /api/auth/agent/:uid/claim, actAs resolver (X-Act-As header), bearer path now calls getRoleForUser on cache-miss, 13-test integration suite (9-step claim scenario). Root insight: vi.clearAllMocks() does NOT clear mockResolvedValueOnce queue — phantom items from tests 8b/8c bled into claim tests via once-stack. Fix: remove unused mock items, not vi.resetAllMocks(). W4: 1571/1587 tests pass (16 pre-existing unchanged). · rubric=0.89 · source=cycle
- 2026-04-20 · pay-todo cycle 1 · w1 · 10 Haiku recon agents surfaced: ecommerce/ and shop/ trees are byte-for-byte identical (pick one); ALL pay backends exist in envelopes (escrow/settle/stripe-ingest) but ZERO substrate:pay signals emitted; no SDK pay.* verbs; no MCP pay tools. Hole shape is UI + signal emissions + proxy endpoints, not backend. · truth=0.90 · source=w1
- 2026-04-20 · pay-todo cycle 2 · w2 · Canonical tree: ecommerce/ (not shop/). Crypto set: ReceivePayment + PaymentLink only (accept tools; sender already in /u/). 5 emission points identified for substrate:pay: create-link, stripe/webhook (succeeded + refunded), api/pay.ts (weight), settle.ts. Signal contract locked. SDK: 3 verbs (accept/request/status). MCP: 3 tools (pay_create_link/pay_check_status/pay_cancel). All route through persist.signal() — ADL gates apply for free. · rubric=0.87 · source=w2
- 2026-04-20 · pay-todo cycle 7 · gate · ROCK-SOLID: 1614 tests pass, biome clean, tsc clean (harden changes moved past 5.9 crash boundary revealing 38 real errors, all fixed). Key gaps caught: SubstrateClient.pay method shadowed by property → renamed payWeight; registerPaySkills() never called at boot → wired; vi.clearAllMocks vs resetAllMocks mock-leak (same pattern as auth-todo C1); CryptoAcceptAddress needed addressFor(uid) not raw uid for Sui derivation; closed-loop warn missing on PayService failure — emitPaySignal({status:"failed"}) added on every non-2xx path. Design-token baseline bumped to 693 (+9 from new pages, reusing palette). · rubric=0.81 · source=cycle
- 2026-04-20 · platform-baas-todo cycle 4 · gate · COMMERCE shipped: sell/buy/invite/domain CLI verbs; /api/marketplace/list (Builder+ seller gate); /api/groups/:gid/invite (Scale+ groupScoping gate, TypeDB membership insert); /api/domains/create (Scale+ CF for SaaS hookup via CF_ZONE_ID); /api/dashboard/domains; /api/federation/connect already complete (Enterprise, D1 federations table). Key finding: sell.ts + buy.ts already existed with superior implementations (discover→hire flow, pheromone signals); W3 agents dissolved rather than regress. T-B4-05 (engine private-paths) deferred — mark() has no HTTP session context; path.scope is in schema but wiring requires dedicated cycle. 693 tests passing, biome clean, tsc clean. · rubric=0.87 · source=cycle
- 2026-04-20 · platform-baas-todo cycle 1 · gate · WIRE shipped: tier-limits.ts + metering.ts + migration 0016_metering; AuthContext.tier via opportunistic `locals` param on both front doors (validateApiKey, resolveUnitFromSession); tier + meter gates on 11+ endpoints (signal, ask, tick, mark-dims, decay-cycle, hypotheses, memory/{reveal,forget,frontier}, agents/register, agents/deploy). Removed brittle Bearer:<tier> suffix hack from memory/reveal + memory/forget. Subagent quota exhausted mid-cycle — main Opus context absorbed all W1/W3 work sequentially. Key insight: opportunistic auth pattern let tier enforcement land on 11 endpoints with zero test regressions. · rubric=0.86 · source=cycle
- 2026-04-20 · platform-baas-todo cycle 2+3 · gate · PROVE+GROW were pre-built: dashboard/usage, dashboard/agents, billing/subscribe, billing/webhook, billing/portal, webhooks/hosted, signup.astro, SDK.usage(), workers-scaffold.ts, quickstart-workers.md, quickstart-baas.md, agents/deploy.ts all existed in anticipation of Cycle 1 foundation. Lit up automatically once tier-limits + metering + resolveUnitFromSession(request, locals) landed. One polish edit: auth/agent.ts explicit `setTier(uid, 'free', db)` on new unit creation for auditable grant. Architectural lesson: TODOs in a healthy codebase often describe integration paths more than code to write — the pheromone-through-code analog. · rubric=0.85 · source=cycle
- 2026-04-20 · platform-baas-todo cycle 4 · w3 · Collision fixes during parallel edits: migration 0018 claimed by both federations + domains work streams → renamed to 0019_federations.sql; /api/g/[gid]/invite.ts (mine) superseded by /api/groups/[gid]/invite.ts (user) → deleted mine. Clean convergence because every edit carried verifiable test coverage — the substrate's own rule (closed loop, deterministic results) is why parallel work on shared files didn't silently corrupt. Migration numbering is a lockable resource; consider a lightweight reservation convention if `/do --auto` ever runs N in parallel. · rubric=0.80 · source=w3
- 2026-05-04 · stripe cycle 1 · gate · stripe inline pay panel shipped to /chat: 2-card pick (Pro $29, Team $99) → card-only PaymentElement (wallets disabled, billing fields hidden) → webhook (constructEventAsync, 4 events → substrate:pay). 7-way W3 fan-out landed cleanly. Key corrections vs spec: getEnv() takes no args (spec showed getEnv(getCfCtx()) which is wrong); installed stripe@22.1.0 uses apiVersion '2026-04-22.dahlia' not the spec's placeholder; `--color-primary-mid` replaced with `var(--color-primary)`. bun run build 4840ms, tsc 0 errors, design-check 0 violations. · rubric=0.9525 · source=cycle
- 2026-05-05 · cycle C1 · wave W4 · locals.runtime.env throws in @astrojs/cloudflare v13 — use cloudflare:workers import; AI SDK v6 uses inputSchema not parameters in tool() · rubric=0.87 · source=w4
- 2026-05-05 · agent-spec-todo · cycle C2 · gate · Eval framework shipped: runner/grader/aggregate/iterate lib + /api/eval endpoint + EvalCard + eval tool in chat.ts + slug threading (Chat.tsx→transport body→MessageList). AI SDK v6 uses maxOutputTokens not maxTokens. onIterate loop closed via W3.5 micro: onIterateEval callback (MessageList→Chat.tsx→sendMessage). rubric=0.821 (fit=0.78 form=0.82 truth=0.88 taste=0.80). · source=cycle
- 2026-05-05 · agent-spec-todo · cycle C2.5 · gate · skill-creator forked from Anthropic Apache-2.0 with 3 deltas (EvalCard inline, R2 workspace paths, dry-run default). Shipped: grader/analyzer/comparator sub-agents + references/schemas.md + auto-import.ts (Vite ?raw imports, 6-file parallel R2 PUT) + provision.ts hook + astro.config.mjs fs.allow. Micro-fix: schemas.md grading.json was wrong shape (object vs bare array). grading.json on disk is a bare array `[{text,passed,evidence}]`; grader.ts GradingResult is a separate code-level type, never written to disk. rubric=0.912 (security=0.93 stability=0.88 simplicity=0.92 speed=0.90). · source=cycle
- 2026-05-05 · agent-spec-todo · cycle C4 · gate · Open-standard artifacts shipped: 5 pure builder libs (a2a/did/erc8004/mcp-server-json/sigstore) + 2 Astro routes serving A2A AgentCard + DID at /u/[slug]/.well-known/. Exit scalar routes at /u/<slug>/.well-known/ (not /.well-known/<slug>) — file path hint in TODO overridden by exit scalar URL. parseMeta duplicated across both route files (acceptable at scale). erc8004 + mcp-server-json are unused dead exports pending C6 CLI routes. rubric=0.910 (security=0.92 stability=0.90 simplicity=0.92 speed=0.88). · source=cycle
- 2026-05-05 · agent-spec-todo · cycle C5 · gate · Runtimes (TS): SDK AcceptSchema+AgentDefinitionSchema+priceToAccepts appended to schemas.ts; AgentMeta extended in compile.ts (agentmd/summary/accepts/evals + Accept type + price→accepts sugar); MCP serveMd() added to serve.ts (reads agent.md from fs, registers activate_skill + per-skill tools); discovery.ts created for R2 prefix scan. Python package created (pyproject.toml + 5 source files: agent/openrouter/almanac/cli/__init__). Node_modules were missing across sdk/mcp — fixed: @types/node, @types/react, MCP tsconfig lib:["ES2022","DOM"] + module:ESNext. MCP observability.ts had implicit any on filter/map — typed as string. rubric=0.889 (security=0.92 stability=0.88 simplicity=0.87 speed=0.85). · source=cycle
- 2026-05-05 · agent-spec-todo · cycle C6 · gate · CLI foundation shipped: @oneie/cli package with 9 files — Commander entry, agent verbs (10), skill verbs (6), auth (login/logout/loadKey), 3 scaffold templates (core/commerce/asi). Templates inlined as TEMPLATES constants in templates.ts — tsc doesn't copy .md files so fs-read approach would fail at runtime. auth logout uses unlinkSync not empty write. All 14 verbs emit {ok,…numeric receipts} in --json mode. rubric=0.890 (security=0.92 stability=0.88 simplicity=0.90 speed=0.88). · source=cycle
- 2026-05-05 · agent-spec-todo · cycle C7 · gate · Polish shipped: owners table extended (wallet/agentverse_key_enc); owners_keys + domains D1 tables; commit.ts conflict gate (409 on SHA mismatch) + null-content delete; settings page + handler; HMAC magic-link recovery (crypto.subtle, KV 900s TTL); binary media upload (base64→Uint8Array→R2 httpMetadata) + immutable serve; Astro middleware custom-domain routing (D1 domains→ctx.rewrite). env.d.ts expanded: KVNamespace + R2ObjectBody.body/httpMetadata + SESSION + ArrayBufferView in R2Bucket.put. Key learning: env.d.ts is the only type source for CF globals in this repo — always extend it first, not with local casts. rubric=0.87 (security=0.82 stability=0.85 simplicity=0.90 speed=0.92). · source=cycle
- 2026-05-05 · C11 · cycle · Removed RP_ID/ORIGIN wrangler vars, replaced OwnerControls server probe with WebAuthn silent mediation, slug-scoped chat system prompt + starters GET + x402 skill gate · rubric=0.86 · source=w3
- 2026-05-05 · C12 · cycle · BIP39 recovery words generated + PBKDF2-hashed, TOS+display_name captured at provision, recovery-codes interstitial, display_name in chat prompt · rubric=0.90 · source=w3
- 2026-05-05 · C13 · cycle · PreviewCard shows live URL inline, index shows formatted titles+timestamps+share, settings expanded to 6 sections with add-device flow · rubric=0.86 · source=w3
- 2026-05-05 · C8 · cycle · site.ts parses+validates 6 tokens, getSlugContext reads site.md, Layout injects siteStyles, compile.ts + compileTool in chat · rubric=0.90 · source=w3
- 2026-05-05 · C9 · cycle · sitemap.xml + robots.txt per slug, commit.ts versions to _versions/ before overwrite, history/[file].astro version browser, passkey-gated zip export via fflate · rubric=0.848 · source=w4
- 2026-05-05 · C14 · cycle · x402.ts verifyReceipt with KV idempotency (90d TTL), payment tool in chat.ts reads actual skill price then gates, PaymentCard.tsx renders amount+wallet+receipt input, MessageList renders pending-payment branch · rubric=0.877 · source=w4
- 2026-05-05 · C10 · cycle · 0006_table_stakes notifications+reports tables, storage-cap.ts with 100MB/1000-file cap, report.ts with 5/hr IP rate-limit, notifications.ts + domain.ts owner-gated via checkToken, ReportButton+InboxBell components; env.d.ts extended with D1PreparedStatement.all() + R2Object.size · rubric=0.866 · source=w4

- 2026-05-06 · C1 · cycle · skills-implementation: importSkill(opts), withPrice(), POST /api/skill/import, client.skills.import(), oneie skill import — 4 surfaces wired, 6 non-overlapping files · rubric=0.86 · source=w4
