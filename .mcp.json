{
  "mcpServers": {
    "one": {
      "command": "npx",
      "args": ["-y", "@oneie/mcp"],
      "env": {
        "ONEIE_API_URL": "https://one.ie",
        "ONEIE_API_KEY": "${ONE_API_KEY}"
      }
    }
  }
}
