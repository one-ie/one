{
  "mcpServers": {
    "one": {
      "command": "sh",
      "args": [
        "-c",
        "set -a; [ -f site/.dev.vars ] && . ./site/.dev.vars; [ -f .env.local ] && . ./.env.local; set +a; exec npx -y @oneie/mcp"
      ]
    }
  }
}
