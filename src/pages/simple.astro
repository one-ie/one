---
import Layout from "../layouts/1.astro";
import { Chat } from "@/components/Chat";
---

<Layout title="Simple Page">
  <div class="h-screen flex flex-col items-center justify-center bg-background text-foreground overflow-hidden">
    <div class="w-full max-w-4xl h-[80vh] rounded-lg overflow-hidden">
      <Chat client:load className="h-full" />
    </div>
  </div>
</Layout>

<style>
  /* Hide scrollbars but keep functionality */
  :global(*::-webkit-scrollbar) {
    display: none;
  }
  
  :global(*) {
    -ms-overflow-style: none;  /* IE and Edge */
    scrollbar-width: none;  /* Firefox */
  }

  /* Remove focus outlines */
  :global(textarea:focus),
  :global(input:focus),
  :global(button:focus),
  :global(*:focus) {
    outline: none !important;
    box-shadow: none !important;
    border-color: transparent !important;
    ring-width: 0 !important;
    ring-color: transparent !important;
  }
  
  /* Remove borders from chat input */
  :global(.chat-input-container *) {
    border: none !important;
    outline: none !important;
  }
  
  :global(.chat-input-container div) {
    border: none !important;
    outline: none !important;
  }
</style>