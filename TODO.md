# TODO — Document the 4 generation files

**Goal:** Document what each file in `one/` generates (web, claw, SDK, etc.) and verify consistency.

**Scope:** `aisdk.md`, `ai-elements.md`, `integrate.md`, `mcp.md` — generation specs, not canonical docs.

**Out of scope:** `design.md` (design system), `routing.md` (substrate routing).

---

## W1 — Understand scope ✅

**Receipt:** files=4 | matches=3 (consistent) | cross_refs=2 (validate) | open_questions=1

### Findings:

**aisdk.md** — Vercel AI SDK spec (streamText ↔ useChat, tool() dispatch, generateObject). Layer 2 of 4. Dual-package (ai + @ai-sdk/react). Provider-agnostic. Consistent with `DSL.md` mark/warn semantics.

**ai-elements.md** — Install 48 React + shadcn components. UI-only for `web/`. Audience: frontend devs. Dependency for `mcp.md` (tools render via canonical `<Tool>` component). **Open:** missing substrate entity/relation mappings.

**integrate.md** — web ↔ claw composition patterns. Two modes (standalone vs. federated with one.ie). Owns health checks, env-var seams, stages 1–5. References mark/warn/highways correctly.

**mcp.md** — MCP server consumption in claw. Turns integrations (Slack, Gmail, GitHub) into runtime tools via `MCP_SERVERS` env. ~50 lines wiring. Consistent with `DSL.md` signal dispatch.

### Cross-ref validation:
- ✅ vs. `dictionary.md`: No conflicts (four docs are *above* substrate level)
- ✅ vs. `DSL.md`: Consistent semantics (mark/warn closes loop, highways routing)
- ⚠️ vs. `one-ontology.md`: ai-elements.md missing entity/relation mappings
- ✅ vs. `.claude/rules/design.md`: design.md stays out of scope; enforcement solid

---

## W2 — Plan generation graph

**Map what each file generates:**

- [ ] **aisdk.md** → generates what? (ai SDK layer, both claw + web?)
- [ ] **ai-elements.md** → generates what? (web components, 48 shadcn + Astro)
- [ ] **integrate.md** → generates what? (web ↔ claw wiring, env-var seams, health checks)
- [ ] **mcp.md** → generates what? (claw tooling, MCP loader, ~50 lines)

**Add to each file:**
- [ ] "Generated by:" — link to which `.ts` or `.tsx` files it controls
- [ ] "Generates:" — list of outputs (components, env-vars, wiring code, tool loaders)
- [ ] "Dependencies:" — which other `one/` files it depends on

---

## W3 — Add generation metadata

**Each file gets a header section:**

```markdown
## Generated by

- `web/src/components/` — ai-elements install script
- `web/src/pages/api/` — aisdk setup
- `claw/src/mcp.ts` — mcp loader
- [list other source files]

## Generates

- [list of files/config that this spec controls]

## Dependencies

- Depends on: [list of other `one/*.md` files]
```

- [ ] Add headers to `aisdk.md`
- [ ] Add headers to `ai-elements.md`
- [ ] Add headers to `integrate.md`
- [ ] Add headers to `mcp.md`

**Then add cross-links:**
- [ ] If docs reference each other, add explicit "See also:" section
- [ ] Link back from code files (comments in `web/src/`, `claw/src/`) to the spec they follow

---

## W4 — Verify generation map

- [ ] **Each file generates its listed outputs:**
  - [ ] `aisdk.md` controls what's in `claw/src/` and `web/` ai-sdk wiring? Spot-check 1 example.
  - [ ] `ai-elements.md` controls `web/src/components/`? Verify 1 component matches spec.
  - [ ] `integrate.md` controls env-var seams and health checks? Spot-check `.env.example` and health-check code.
  - [ ] `mcp.md` controls `src/mcp.ts`? Spot-check loader pattern.

- [ ] **Dependencies are accurate:**
  - [ ] No circular references (A depends on B depends on A)
  - [ ] Transitive deps are listed
  - [ ] External deps (dictionary.md, DSL.md) noted if referenced

- [ ] **Code comments link back to specs:**
  ```bash
  grep -r "See one/aisdk.md\|See one/ai-elements.md\|See one/integrate.md\|See one/mcp.md" \
    web/ claw/ sdk/ 2>/dev/null | wc -l
  ```
  - [ ] At least 1 backreference per doc

- [ ] **Rubric gate:**
  - **Fit (0.92+):** Each doc says what it generates; generation map is complete
  - **Form (0.85+):** Headers consistent; no dead links to code files
  - **Truth (0.90+):** Generated code matches spec (spot-checks pass)
  - **Taste (0.80+):** Consistent structure across 4 files

---

## Exit & pheromone

**Expected outcome:** 4 generation specs with traceability headers

Close with:
- [ ] Commit: `docs: add generation headers to one/*.md specs`
- [ ] Each file has "Generated by:" + "Generates:" + "Dependencies:" section
- [ ] Spot-check 4 code examples match specs (1 per file)

**Final numbers:**
- Docs mapped: 4 (aisdk, ai-elements, integrate, mcp)
- Headers added: 4 (one per file)
- Code backrefs: 4+ (linking from source to spec)
- Rubric target: fit≥0.92, form≥0.85, truth≥0.90, taste≥0.80
