node_modules/
dist/
.astro/
.wrangler/
create/template/
create/dist/
coverage/
*.tsbuildinfo
.turbo/

# secrets
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
!.dev.vars.example
*.key
*.pem

# ONE team's own reference deploy config — never shipped to a template clone
wrangler.template-deploy.toml

# editor / OS
.DS_Store
*.log

# linked worktrees
.worktrees/

# /do harness scratch — wave handoff state, never shipped
.w2-*.json
.w3-*.json
